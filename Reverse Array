
#include <iostream>

using namespace std;
void reverseArray(int arr[],int size)
{
    for(int i=size-1; i>=0; i--)
    {
        cout<<arr[i]<<" ";
    }
}
int main()
{
    int arr[]={10,4,3,6,3,1};
    int n=sizeof(arr)/sizeof(arr[0]);
    reverseArray(arr,n);

    return 0;
}
